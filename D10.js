/* Questo array viene usato per gli esercizi. Non modificarlo. */
const movies = [
  {
    Title: "The Lord of the Rings: The Fellowship of the Ring",
    Year: "2001",
    imdbID: "tt0120737",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BN2EyZjM3NzUtNWUzMi00MTgxLWI0NTctMzY4M2VlOTdjZWRiXkEyXkFqcGdeQXVyNDUzOTQ5MjY@._V1_SX300.jpg",
  },

  {
    Title: "The Lord of the Rings: The Return of the King",
    Year: "2003",
    imdbID: "tt0167260",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BNzA5ZDNlZWMtM2NhNS00NDJjLTk4NDItYTRmY2EwMWZlMTY3XkEyXkFqcGdeQXVyNzkwMjQ5NzM@._V1_SX300.jpg",
  },
  {
    Title: "The Lord of the Rings: The Two Towers",
    Year: "2002",
    imdbID: "tt0167261",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BNGE5MzIyNTAtNWFlMC00NDA2LWJiMjItMjc4Yjg1OWM5NzhhXkEyXkFqcGdeQXVyNzkwMjQ5NzM@._V1_SX300.jpg",
  },
  {
    Title: "Lord of War",
    Year: "2005",
    imdbID: "tt0399295",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BMTYzZWE3MDAtZjZkMi00MzhlLTlhZDUtNmI2Zjg3OWVlZWI0XkEyXkFqcGdeQXVyNDk3NzU2MTQ@._V1_SX300.jpg",
  },
  {
    Title: "Lords of Dogtown",
    Year: "2005",
    imdbID: "tt0355702",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BNDBhNGJlOTAtM2ExNi00NmEzLWFmZTQtYTZhYTRlNjJjODhmXkEyXkFqcGdeQXVyNDk3NzU2MTQ@._V1_SX300.jpg",
  },
  {
    Title: "The Lord of the Rings",
    Year: "1978",
    imdbID: "tt0077869",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BOGMyNWJhZmYtNGQxYi00Y2ZjLWJmNjktNTgzZWJjOTg4YjM3L2ltYWdlXkEyXkFqcGdeQXVyNTAyODkwOQ@@._V1_SX300.jpg",
  },
  {
    Title: "Lord of the Flies",
    Year: "1990",
    imdbID: "tt0100054",
    Type: "movie",
    Poster: "https://m.media-amazon.com/images/M/MV5BOTI2NTQyODk0M15BMl5BanBnXkFtZTcwNTQ3NDk0NA@@._V1_SX300.jpg",
  },
  {
    Title: "The Lords of Salem",
    Year: "2012",
    imdbID: "tt1731697",
    Type: "movie",
    Poster: "https://m.media-amazon.com/images/M/MV5BMjA2NTc5Njc4MV5BMl5BanBnXkFtZTcwNTYzMTcwOQ@@._V1_SX300.jpg",
  },
  {
    Title: "Greystoke: The Legend of Tarzan, Lord of the Apes",
    Year: "1984",
    imdbID: "tt0087365",
    Type: "movie",
    Poster: "https://m.media-amazon.com/images/M/MV5BMTM5MzcwOTg4MF5BMl5BanBnXkFtZTgwOTQwMzQxMDE@._V1_SX300.jpg",
  },
  {
    Title: "Lord of the Flies",
    Year: "1963",
    imdbID: "tt0057261",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BOGEwYTlhMTgtODBlNC00ZjgzLTk1ZmEtNmNkMTEwYTZiM2Y0XkEyXkFqcGdeQXVyMzU4Nzk4MDI@._V1_SX300.jpg",
  },
  {
    Title: "The Avengers",
    Year: "2012",
    imdbID: "tt0848228",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BNDYxNjQyMjAtNTdiOS00NGYwLWFmNTAtNThmYjU5ZGI2YTI1XkEyXkFqcGdeQXVyMTMxODk2OTU@._V1_SX300.jpg",
  },
  {
    Title: "Avengers: Infinity War",
    Year: "2018",
    imdbID: "tt4154756",
    Type: "movie",
    Poster: "https://m.media-amazon.com/images/M/MV5BMjMxNjY2MDU1OV5BMl5BanBnXkFtZTgwNzY1MTUwNTM@._V1_SX300.jpg",
  },
  {
    Title: "Avengers: Age of Ultron",
    Year: "2015",
    imdbID: "tt2395427",
    Type: "movie",
    Poster:
      "https://m.media-amazon.com/images/M/MV5BMTM4OGJmNWMtOTM4Ni00NTE3LTg3MDItZmQxYjc4N2JhNmUxXkEyXkFqcGdeQXVyNTgzMDMzMTg@._V1_SX300.jpg",
  },
  {
    Title: "Avengers: Endgame",
    Year: "2019",
    imdbID: "tt4154796",
    Type: "movie",
    Poster: "https://m.media-amazon.com/images/M/MV5BMTc5MDE2ODcwNV5BMl5BanBnXkFtZTgwMzI2NzQ2NzM@._V1_SX300.jpg",
  },
];
/*
REGOLE
- Tutte le risposte devono essere scritte in JavaScript
- Se sei in difficoltà puoi chiedere aiuto a un Teaching Assistant
- Puoi usare Google / StackOverflow ma solo quanto ritieni di aver bisogno di qualcosa che non è stato spiegato a lezione
- Puoi testare il tuo codice in un file separato, o de-commentando un esercizio alla volta
- Per farlo puoi utilizzare il terminale Bash, quello di VSCode o quello del tuo sistema operativo (se utilizzi macOS o Linux)
*/

// JS Basics

/* ESERCIZIO A
  Crea una variabile chiamata "sum" e assegnaci il risultato della somma tra i valori 10 e 20.
*/
const sum = 10 + 20;
console.log("esercizio a : ", sum);
console.log("-----------------------------------------------------------");

/* ESERCIZIO B
  Crea una variabile chiamata "random" e assegnaci un numero casuale tra 0 e 20 (deve essere generato dinamicamente a ogni esecuzione).
*/

const random = Math.floor(Math.random() * 21);
console.log("esercizio b", random);
console.log("-----------------------------------------------------------");

/* ESERCIZIO C
  Crea una variabile chiamata "me" e assegnaci un oggetto contenente le seguenti proprietà: name = il tuo nome, surname = il tuo cognome, age = la tua età.
*/
const me = {
  name: "alessandro",
  surname: "baiocco",
  age: 25,
};
console.log("esercizio c", me);
console.log("-----------------------------------------------------------");

/* ESERCIZIO D
  Crea del codice per rimuovere programmaticamente la proprietà "age" dall'oggetto precedentemente creato.
*/
delete me.age;
console.log("eserzizio d", me);
console.log("-----------------------------------------------------------");

/* ESERCIZIO E
  Crea del codice per aggiungere programmaticamente all'oggetto precedentemente creato un array chiamato "skills", contenente i linguaggi di programmazione che conosci.
*/
me.skill = ["html", "css"];
console.log("esercizio E", me);
console.log("-----------------------------------------------------------");

/* ESERCIZIO F
  Crea un pezzo di codice per aggiungere un nuovo elemento all'array "skills" contenuto nell'oggetto "me".
*/
me.skill.push("js");
console.log("esercizio f:", me);
console.log("-----------------------------------------------------------");

/* ESERCIZIO G
  Crea un pezzo di codice per rimuovere programmaticamente l'ultimo elemento dall'array "skills" contenuto nell'oggetto "me".
*/
me.skill.pop();
console.log("esercizio G", me);

console.log("-----------------------------------------------------------");

// Funzioni

/* ESERCIZIO 1
  Crea una funzione chiamata "dice": deve generare un numero casuale tra 1 e 6.
*/
console.log("esercizio 1");
const dice = () => {
  rollTheDice = Math.floor(Math.random() * 6 + 1);
  console.log(rollTheDice);
  return rollTheDice;
};

dice();

console.log("-----------------------------------------------------------");

/* ESERCIZIO 2
  Crea una funzione chiamata "whoIsBigger" che riceve due numeri come parametri e ritorna il maggiore dei due.
*/
const whoIsBigger = (a, b) => {
  if (a > b) {
    console.log("esercizio 2:", a, "è maggiore di ", b);
    return a;
  } else if (b > a) {
    console.log("esercizio 2:", b, "è maggiore di ", a);
    return b;
  } else {
    console.log("esercizio 2:", a, "e", b, "sono uguali");
    return a, b;
  }
};
whoIsBigger(8, 9);

console.log("-----------------------------------------------------------");

/* ESERCIZIO 3
  Crea una funzione chiamata "splitMe" che riceve una stringa come parametro e ritorna un'array contenente ogni parola della stringa.

  Es.: splitMe("I love coding") => ritorna ["I", "Love", "Coding"]
*/
const splitMe = (str) => {
  let splitted = str.split(" ");
  console.log("esercizio 3", splitted);
  return splitted;
};

splitMe("pippo is awesome");
console.log("-----------------------------------------------------------");

/* ESERCIZIO 4
  Crea una funzione chiamata "deleteOne" che riceve una stringa e un booleano come parametri.
  Se il valore booleano è true la funzione deve ritornare la stringa senza il primo carattere, altrimenti la deve ritornare senza l'ultimo.
*/

const deleteOne = (str, bool) => {
  if (bool) {
    const minusOne = str.substring(1);
    console.log("esecizio 4:", minusOne);
  } else {
    const minusLast = str.slice(0, -1);
    console.log("esecizio 4:", minusLast);
  }
};
deleteOne("paperino", true);

console.log("-----------------------------------------------------------");

/* ESERCIZIO 5
  Crea una funzione chiamata "onlyLetters" che riceve una stringa come parametro e la ritorna eliminando tutte le cifre numeriche.

  Es.: onlyLetters("I have 4 dogs") => ritorna "I have dogs"
*/
const numericNumber = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0];
const onlyLetters = (str) => {
  finalStr = str.replace(/[0-9]/g, "");
  console.log("esercizio 5", finalStr);
};

onlyLetters("I have 4 dogs");
console.log("-----------------------------------------------------------");
// }

/* ESERCIZIO 6
  Crea una funzione chiamata "isThisAnEmail" che riceve una stringa come parametro e ritorna true se la stringa è un valido indirizzo email.
*/
const isThisAnEmail = (str) => {
  const test = str.split("");
  if (test.includes("@")) {
    console.log("esercizio 6 : la mail è valida");
    return true;
  } else {
    console.log("esercizio 6 :la mail non è valida");
    return false;
  }
};
isThisAnEmail("ciao@gmail");

console.log("-----------------------------------------------------------");

/* ESERCIZIO 7
  Scrivi una funzione chiamata "whatDayIsIt" che ritorna il giorno della settimana corrente.
  */
const today = new Date();
const whatDayIsIt = () => {
  const dayDate = today.getDay();
  switch (true) {
    case dayDate === 0:
      console.log("esercizio 7 : oggi è domenica");
      break;
    case dayDate === 1:
      console.log("esercizio 7 : oggi è lunedi");
      break;
    case dayDate === 2:
      console.log("esercizio 7 : oggi è martedi");
      break;
    case dayDate === 3:
      console.log("esercizio 7 : oggi è mercoledi");
      break;
    case dayDate === 4:
      console.log("esercizio 7 : oggi è giovedi");
      break;
    case dayDate === 5:
      console.log("esercizio 7 : oggi è venerdi");
      break;
    case dayDate === 6:
      console.log("esercizio 7 : oggi è sabato");
      break;
  }
};

whatDayIsIt();

console.log("-----------------------------------------------------------");
/* ESERCIZIO 8
  Scrivi una funzione chiamata "rollTheDices" che riceve un numero come parametro.
  Deve invocare la precedente funzione dice() il numero di volte specificato nel parametro, e deve tornare un oggetto contenente una proprietà "sum":
  il suo valore deve rappresentare il totale di tutti i valori estratti con le invocazioni di dice().
  L'oggetto ritornato deve anche contenere una proprietà "values", contenente un array con tutti i valori estratti dalle invocazioni di dice().

  Example:
  rollTheDices(3) => ritorna {
      sum: 10
      values: [3, 3, 4]
  }
*/

const rollTheDices = (n) => {
  let totSum = 0;
  const values = [];
  for (i = 0; i < n; i++) {
    const value = dice();
    values.push(value);
    totSum += value;
  }
  console.log("esercizio 8", totSum, values);
};

rollTheDices(3);
console.log("-----------------------------------------------------------");

/* ESERCIZIO 9
  Scrivi una funzione chiamata "howManyDays" che riceve una data come parametro e ritorna il numero di giorni trascorsi da tale data.
*/
const dateExer9 = new Date("December 17, 2022");
let timePassed = 0;
const howManyDays = (newdate) => {
  timePassed = today - newdate;
  const day = Math.floor(timePassed / (1000 * 60 * 60 * 24));
  console.log("esercizio 9 : it's passed ", day, "days");
};

howManyDays(dateExer9);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 10
  Scrivi una funzione chiamata "isTodayMyBirthday" che deve ritornare true se oggi è il tuo compleanno, falso negli altri casi.
*/

const isTodayMyBirthday = () => {
  numberOfDay = today.getDate();
  numberOfMonth = today.getMonth();
  if (numberOfDay === 5 && numberOfMonth === 9) {
    console.log("esercizio 10 : ", true);
  } else {
    console.log("esercizio 10: ", false);
  }
};

isTodayMyBirthday();

console.log("-----------------------------------------------------------");
// Arrays & Oggetti

// NOTA: l'array "movies" usato in alcuni esercizi è definito alla fine di questo file
/* ESERCIZIO 11
  Scrivi una funzione chiamata "deleteProp" che riceve un oggetto e una stringa come parametri; deve ritornare l'oggetto fornito dopo aver eliminato
  in esso la proprietà chiamata come la stringa passata come secondo parametro.
*/
const deleteProp = (obj, str) => {
  let output = delete obj[str];
  console.log("esercizio 11", me);
};

deleteProp(me, "name");

console.log("-----------------------------------------------------------");
/* ESERCIZIO 12
  Scrivi una funzione chiamata "newestMovie" che trova il film più recente nell'array "movies" fornito.
*/
let yearNewestMovie = 0;
const newestMovie = () => {
  for (i = 0; i < movies.length; i++) {
    if (movies[i].Year > yearNewestMovie) {
      yearNewestMovie = movies[i].Year;
    }
  }
  console.log("esercizio 12", yearNewestMovie);
};
newestMovie();

console.log("-----------------------------------------------------------");

/* ESERCIZIO 13
  Scrivi una funzione chiamata countMovies che ritorna il numero di film contenuti nell'array "movies" fornito.
*/
const countMovies = () => {
  count = 0;
  for (i = 0; i < movies.length; i++) {
    count += 1;
  }
  console.log("esercizio 13", count);
};
countMovies();
console.log("-----------------------------------------------------------");

/* ESERCIZIO 14
  Scrivi una funzione chiamata "onlyTheYears" che crea un array con solamente gli anni di uscita dei film contenuti nell'array "movies" fornito.
*/

const yearArray = [];
const onlyTheYears = () => {
  for (j = 0; j < movies.length; j++) {
    yearArray.push(movies[j].Year);
  }
  console.log("esercizio 14 : ", yearArray);
};

onlyTheYears();

console.log("-----------------------------------------------------------");

/* ESERCIZIO 15
  Scrivi una funzione chiamata "onlyInLastMillennium" che ritorna solamente i film prodotto nel millennio scorso contenuti nell'array "movies" fornito.
*/
lastMilleniumMovies = [];
const onlyInLastMillennium = () => {
  for (k = 0; k < movies.length; k++) {
    if (movies[k].Year <= 2000 && movies[k].Year > 1000) {
      //maggiore di 1000 non serviva ma volevo metterlo
      lastMilleniumMovies.push(movies[k].Year);
    }
  }
  console.log("esercizio 15 :", lastMilleniumMovies);
};

onlyInLastMillennium();

console.log("-----------------------------------------------------------");

/* ESERCIZIO 16
  Scrivi una funzione chiamata "sumAllTheYears" che ritorna la somma di tutti gli anni in cui sono stati prodotti i film contenuti nell'array "movies" fornito.
*/

const sumAllTheYears = () => {
  const yearSum = yearArray.reduce((accumulator, actualValue) => accumulator + (actualValue - 0), 0);
  console.log("esercizio 16 : ", yearSum);
};
sumAllTheYears();

console.log("-----------------------------------------------------------");

/* ESERCIZIO 17
  Scrivi una funzione chiamata "searchByTitle" che riceve una stringa come parametro e ritorna i film nell'array "movies" fornito che la contengono nel titolo.
*/
const found = [];
const searchByTitle = (str) => {
  for (l = 0; l < movies.length; l++) {
    if (movies[l].Title.search(str) !== -1) {
      found.push(movies[l]);
    }
  }
  console.log("esercizio 17 :", found);
};
searchByTitle("Lord");
console.log("-----------------------------------------------------------");

/* ESERCIZIO 18
  Scrivi una funzione chiamata "searchAndDivide" che riceve una stringa come parametro e ritorna un oggetto contenente due array: "match" e "unmatch".
  "match" deve includere tutti i film dell'array "movies" fornito che contengono la stringa fornita all'interno del proprio titolo, mentre "unmatch" deve includere tutti i rimanenti.
*/
const match = [];
const unmatch = [];
const searchAndDivide = (str) => {
  for (l = 0; l < movies.length; l++) {
    if (movies[l].Title.search(str) !== -1) {
      match.push(movies[l]);
    } else {
      unmatch.push(movies[l]);
    }
  }
  console.log("esercizio 18 :", match, unmatch);
};
searchAndDivide("Lord");

console.log("-----------------------------------------------------------");

/* ESERCIZIO 19
  Scrivi una funzione chiamata "removeIndex" che riceve un numero come parametro e ritorna l'array "movies" fornito privo dell'elemento nella posizione ricevuta come parametro.
*/
const removeIndex = (num) => {
  if (num === 0) {
    for (p = 0; p < movies.length; p++) {
      delete movies[p].Title;
    }
  } else if (num === 1) {
    for (p = 0; p < movies.length; p++) {
      delete movies[p].Year;
    }
  } else if (num === 2) {
    for (p = 0; p < movies.length; p++) {
      delete movies[p].Type;
    }
  } else if (num === 3) {
    for (p = 0; p < movies.length; p++) {
      delete movies[p].imdbID;
    }
  } else if (num === 4) {
    for (p = 0; p < movies.length; p++) {
      delete movies[p].Poster;
    }
  }
  console.log("esercizio 19 :", movies);
};
removeIndex(2);

console.log("-----------------------------------------------------------");

// DOM (nota: gli elementi che selezionerai non si trovano realmente nella pagina)

/* ESERCIZIO 20
  Scrivi una funzione per selezionare l'elemento dotato di id "container" all'interno della pagina.
*/
const esercizio20 = document.getElementById("container");
console.log("esercizio 20 :", esercizio20);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 21
  Scrivi una funzione per selezionare ogni tag <td> all'interno della pagina.
*/
const esercizio21 = document.getElementsByTagName("td");
console.log("esercizio 21 :", esercizio21);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 22
  Scrivi una funzione che, tramite un ciclo, stampa in console il testo contenuto in ogni tag <td> all'interno della pagina.
*/
console.log("esercizio22");
for (let i = 0; i < esercizio21.length; i++) {
  console.log(esercizio21[i].innerText);
}

console.log("-----------------------------------------------------------");
/* ESERCIZIO 23
  Scrivi una funzione per aggiungere un background di colore rosso a ogni link all'interno della pagina.
*/
const esercizio23 = document.querySelectorAll("a");
for (let i = 0; i < esercizio23.length; i++) {
  esercizio23[i].style = "background-color : red";
}
console.log("esercizio 23 :", esercizio23);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 24
  Scrivi una funzione per aggiungere un nuovo elemento alla lista non ordinata con id "myList".
*/
const esercizio24 = document.getElementById("myList");
const esercizio24_2 = document.createElement("li");
esercizio24_2.innerText = "questo è creato dal JS";
esercizio24.appendChild(esercizio24_2);
console.log("esercizio 24 :", esercizio24);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 25
  Scrivi una funzione per svuotare la lista non ordinata con id "myList".
*/
esercizio24.innerHTML = "";
console.log("esercizio 25 : gli elementi sono stati elminati", esercizio24);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 26
  Scrivi una funzione per aggiungere ad ogni tag <tr> la classe CSS "test"
*/
const esercizio26 = document.querySelectorAll("tr");
for (let i = 0; i < esercizio26.length; i++) {
  esercizio26[i].classList.add("test");
}
console.log("esercizio 26 :", esercizio26);

console.log("-----------------------------------------------------------");
// [EXTRA] JS Avanzato

/* ESERCIZIO 27
  Crea una funzione chiamata "halfTree" che riceve un numero come parametro e costruisce un mezzo albero di "*" (asterischi) dell'altezza fornita.

  Esempio:
  halfTree(3)

  *
  **
  ***

*/
console.log("esercizio 27 :");
const halfTree = (num) => {
  for (let i = 0; i <= num; i++) {
    let star = "";
    for (let j = 0; j <= i; j++) {
      star += "*";
    }
    console.log(star);
  }
};

halfTree(5);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 28
  Crea una funzione chiamata "tree" che riceve un numero come parametro e costruisce un albero di "*" (asterischi) dell'altezza fornita.

  Esempio:
  tree(3)

    *
   ***
  *****

*/
const tree = (num) => {};

tree(3);

console.log("-----------------------------------------------------------");
/* ESERCIZIO 29
  Crea una funzione chiamata "isItPrime" che riceve un numero come parametro e ritorna true se il numero fornito è un numero primo.
*/

const primeNumber = [2, 3, 5, 7, 11];
const isItPrime = (num) => {
  if (primeNumber.includes(num)) {
    console.log("esercizio 29 :", num, "è un numero primo", true);
  } else if (num % 2 === 0 || num % 3 === 0 || num % 5 === 0 || num % 7 === 0 || num % 11 === 0) {
    console.log("esercizio 29 :", num, "non è un numero primo", false);
  } else {
    console.log("esercizio 29 :", num, "è un numero primo", true);
  }
};

isItPrime(25);
